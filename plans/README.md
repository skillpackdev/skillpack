# Implementation Plans

Generated by the improve skill on 2026-07-10 against commit `f999925`. Execute in
the order below. Each executor must read its plan fully, honor STOP conditions,
run every verification gate, and update the status row when done.

## Execution order and status

| Plan                                                        | Title                                                  | Finding | Priority | Effort | Depends on    | Status |
| ----------------------------------------------------------- | ------------------------------------------------------ | ------- | -------- | ------ | ------------- | ------ |
| [001](001-remove-mcp-delete-action.md)                      | Remove destructive Skill deletion from MCP             | I-01    | P1       | S      | —             | TODO   |
| [002](002-preserve-skill-frontmatter.md)                    | Preserve custom frontmatter in full SKILL.md mutations | I-02    | P1       | M      | 001           | TODO   |
| [003](003-use-action-specific-manage-skill-schema.md)       | Publish an action-specific `manage_skill` input schema | I-04    | P1       | S–M    | 001, 002      | TODO   |
| [004](004-guard-skill-patches-against-concurrent-writes.md) | Guard Skill patches against concurrent head changes    | I-05    | P2       | M      | 001, 002, 003 | TODO   |

Status values: `TODO` | `IN PROGRESS` | `DONE` | `BLOCKED: <reason>` |
`REJECTED: <reason>`

## Dependency notes

- Plan 001 settles the supported MCP action set before Plan 003 publishes an action-discriminated schema.
- Plan 002 and Plan 003 both restructure `manage-skill.ts`; sequential execution keeps frontmatter preservation intact while the input schema changes.
- Plan 004 runs last because it touches the same MCP handlers plus the deeper Skill service/repository write path. Its tests must retain all Plan 001–003 regressions.
- Each plan's drift check explicitly recognizes completed predecessor changes. Any additional semantic drift is a STOP condition.

## Findings considered and rejected

- **I-03 — preserve `create_skill` / `update_skill` compatibility aliases**: rejected by the maintainer on 2026-07-10. MCP connections are generally short-lived, so direct cutover to `manage_skill` is accepted. Executors must not reintroduce aliases in these plans.

## Deferred findings

- **P-01 — upgrade Better Auth for GHSA-86j7-9j95-vpqj**: confirmed high-severity and reachable through the OAuth Provider path during the branch audit. It was outside the maintainer's selected I-01/I-02/I-04/I-05 planning set and remains unplanned.

## Shared repository gates

Every plan uses the same final gates:

```bash
pnpm check
pnpm typecheck
pnpm test
pnpm build
git diff --check
git status --short
```

Expected result: all commands exit 0; `git diff --check` prints nothing; status
contains only the plan's declared in-scope files and `plans/README.md` when its
status row is updated.

## Shared boundaries

- Follow `docs/backend-architecture.md`: MCP files own protocol concerns, Skill services own business flows, repositories own D1 access, and storage helpers own R2 access.
- Use `@server/*` for cross-module or deep server imports.
- Keep REST contracts in `@skillpack/contracts/*`; internal MCP/service preconditions stay server-internal.
- Use D1 `batch()` and parameterized Drizzle/SQL. Do not introduce Drizzle `transaction()` on the current-state Skill write path.
- Preserve domain terms from `CONTEXT.md`: Managed Skill, Skill Name, Skill Version, Skill Version History, Resource Manifest, and Skill File.
- Do not commit secrets, `.dev.vars` contents, generated `dist/`, or local Wrangler state.
- Do not push, open a PR, or commit unless the operator explicitly requests it.

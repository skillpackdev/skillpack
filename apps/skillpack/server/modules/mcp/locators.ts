import {
  parseSkillResourceUri,
  skillIndexUri,
  toSkillLocation,
  toSkillResourceUri,
} from "@skillpack/core/skill-locations";

export { skillIndexUri };

export const toSkillpackResourceUri = toSkillResourceUri;
export const toSkillpackLocation = toSkillLocation;
export const parseSkillpackResourceUri = parseSkillResourceUri;
